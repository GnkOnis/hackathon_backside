create table maintable(
     id VARCHAR(26) NOT NULL primary key,
     title varchar(256),
     category int,
     curr int,
     link varchar(256),
     createtime DATETIME,
     updatetime DATETIME,
     numcomment int,
     summary varchar(2048),
     name varchar(64)
);
